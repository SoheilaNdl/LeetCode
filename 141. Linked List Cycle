        fast = ListNode(head)
        slow = ListNode(head)
        fast = head
        slow = head
        while(fast):
            fast = fast.next.next
            slow = slow.next
            if fast is slow:
                return True
        return False
